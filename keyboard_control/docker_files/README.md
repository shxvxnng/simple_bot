### docker_files

```diff
+ Instructions to create the docker image and create and run a container using this image:
+ Build the docker image using:
docker build -t osrf/ros2:foxy-desktop-nvidia-gazebo-nav2 .
+ Create and run a container using this image using:
bash run.bash
```
